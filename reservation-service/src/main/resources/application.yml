server:
  port: 8083

spring:
  application:
    name: reservation-service

  datasource:
    url: jdbc:mysql://localhost:3306/hotel_reservation_db?createDatabaseIfNotExist=true
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
